@model IEnumerable<BookViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Books Table</h3>
    </div>
    <div class="card-body">
        
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th style="width: 10px">#</th>
                        <th>Image</th>
                        <th>Book Name</th>
                        <th>Book Description</th>
                        <th>UPDATE</th>
                    </tr>
                </thead>
                <tbody>
                @if (Model.Count() > 0)
                {
                    var itr = 1;
                    @foreach (var book in Model)
                    {
                        <tr>
                            <td>@itr++</td>
                            <td><img src="" class="rounded" height="50" width="50" alt="Book's Image'" /></td>
                            <td>@book.Name</td>
                            <td>@book.Discription</td>
                            <td>@book.Discription</td>
                            <td><a asp-action="Edit" asp-route-id="@book.Id"><i class="fas fa-edit"></i></a></td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted">
                            No books found.
                        </td>
                    </tr>
                }
                </tbody>
            </table>
    </div>
</div>